# Use the public npm registry
registry=https://registry.npmjs.org/

# OPTIONAL: If you publish to scoped registries, add a scope-specific registry:
# @your-scope:registry=https://registry.example.com/

# OPTIONAL: If you need an auth token for the registry, use an environment variable (do NOT hardcode tokens)
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}

# Example for GitHub Packages (if you use GH packages for @your-scope)
# @your-scope:registry=https://npm.pkg.github.com/
# //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

# Safety notes:
# - Never commit a literal token in this file. Use environment variables (NPM_TOKEN, GITHUB_TOKEN) or CI secrets.
# - Project-level .npmrc should live in your project root (card/.npmrc). Global .npmrc lives at ~/.npmrc
